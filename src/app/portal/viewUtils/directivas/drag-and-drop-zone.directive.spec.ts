import { DragAndDropZoneDirective } from './drag-and-drop-zone.directive';

describe('DragAndDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
